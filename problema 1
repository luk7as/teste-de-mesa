using System;

namespace testeDeMesa_1
{
internal class Program
{
static void Main(string[] args)
{
int a = 10;
Console.WriteLine("valor de a : {0}", a);

        int b = 20;
        Console.WriteLine("valor de b : {0}", b);

        int c = (a + b) / 2;
        Console.WriteLine("valor de c : {0}", c);

        c = c - 40;
        Console.WriteLine("valor de c alterado : {0}", c);

        int[] numeros = new int[3] { a, b, c };
        Console.ReadLine();

    }
}
}
